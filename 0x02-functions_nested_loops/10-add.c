#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * Description: adds two integers.
 * @a: first input
 * @b: second input
 * Return: sum of a & b
*/

int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
